import React from "react";

const RightSection = ({ notes }) => {
  //   console.log("notesData", notes);
  return (
    <div>
      {/* {notes?.map((ele) => (
        <div key={ele.groupName}>
          {ele?.notes?.map((note, i) => (
            <p key={i}>{note.note}</p>
          ))}
        </div>
      ))} */}
      right section
    </div>
  );
};

export default RightSection;
